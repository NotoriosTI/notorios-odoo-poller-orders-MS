# Path a la base de datos SQLite
POLLER_DB_PATH=data/poller.db

# Nivel de logging: DEBUG, INFO, WARNING, ERROR
POLLER_LOG_LEVEL=INFO

# Clave maestra Fernet para encriptar API keys (generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
POLLER_ENCRYPTION_KEY=

# URL por defecto para webhooks de StockMaster
POLLER_DEFAULT_WEBHOOK_URL=
